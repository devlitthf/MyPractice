const naam = "Devraj"
let daata = 123
var alag = 444
different = "Location"
let location;


// naam = "Raikwar" Not allowed because of 
// console.log(naam)

/**
 * Prefer not to use [var] because of issue in block scope and functional scope
 */
console.table([naam, daata, alag, different, location])